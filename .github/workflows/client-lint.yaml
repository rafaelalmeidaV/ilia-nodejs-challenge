name: Client Service Lint

on:
  pull_request:
    paths:
      - 'client-service/**'
  push:
    branches:
      - main
      - 'feature/**'
    paths:
      - 'client-service/**'

jobs:
  lint:
    name: Lint Client Service
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: client

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: client-service/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run lint
        run: npm run lint
